import styled from 'styled-components';

export const Container = styled.header`
    background: var(--dark);
    padding: 2rem;

    h2{
        color: var(--shape);
        font-weight: 500;
    }
`;